# **AMR Project Scripts**
This repository contains the python and bash scripts used for AMR gene sequence retrieval

Requirements:
- conda / miniconda3
- Dependencies in environment.yml:
  - conda env create -f environment.yml
  
Pipeline: `python gene_retriever -h`
